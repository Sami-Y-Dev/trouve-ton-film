{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un film{% endblock %}

{% block body %}
    <h2>Ajouter un nouveau film</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_movie_list') }}">Retour à la liste</a>
{% endblock %}

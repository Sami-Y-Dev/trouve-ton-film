{% extends 'admin/base.html.twig' %}

{% block title %}Modifier le film{% endblock %}

{% block body %}
    <h2>Modifier le film : {{ movie.title }}</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_movie_list') }}">Retour à la liste</a>
{% endblock %}
